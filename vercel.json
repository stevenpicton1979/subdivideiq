{
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"preview\" ] && exit 0; ! git diff HEAD^ HEAD --name-only -- . ':(exclude)*.md' ':(exclude)*.txt' ':(exclude)docs/' ':(exclude)data/' ':(exclude)STATE.md' ':(exclude)BACKLOG.md' ':(exclude)OVERNIGHT_LOG.md' | grep -q .",
  "version": 2,
  "buildCommand": "echo 'No build step required'",
  "outputDirectory": "public",
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-store"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "Content-Type",
          "value": "application/json; charset=utf-8"
        }
      ]
    }
  ]
}
